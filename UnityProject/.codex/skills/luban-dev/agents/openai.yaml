interface:
  display_name: "Luban Configuration"
  short_description: "Validate and export TEngine configuration and table shards"
  default_prompt: "Use $luban-dev to make this configuration change and validate its export."
policy:
  allow_implicit_invocation: true
